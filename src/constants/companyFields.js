const REQUIRED_COMPANY_NUMERIC_FIELDS = Object.freeze([
  'price',
  'eps',
  'bookValuePerShare',
  'annualDividend',
  'fcf',
  'sharesOutstanding',
  'netDebt',
]);

module.exports = {
  REQUIRED_COMPANY_NUMERIC_FIELDS,
};
